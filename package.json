{
  "name": "selftaught",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@12.4.1",
  "scripts": {
    "dev:admin": "dotenv -e .env -- pnpm --filter @selftaught/admin dev",
    "dev:frontend": "dotenv -e .env -- pnpm --filter @selftaught/frontend dev",
    "build:admin": "dotenv -e .env -- pnpm --filter @selftaught/admin build",
    "build:frontend": "dotenv -e .env -- pnpm --filter @selftaught/frontend build",
    "db:generate": "pnpm --filter @selftaught/core db:generate",
    "db:migrate": "dotenv -e .env -- pnpm --filter @selftaught/core db:migrate",
    "db:seed": "dotenv -e .env -- pnpm --filter @selftaught/core db:seed",
    "db:studio": "dotenv -e .env -- pnpm --filter @selftaught/core db:studio",
    "lint": "eslint .",
    "typecheck": "pnpm -r --parallel typecheck",
    "test": "dotenv -e .env -- vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "dotenv-cli": "^8.0.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.19.0",
    "vitest": "^2.1.8",
    "vue-tsc": "^2.2.0"
  }
}
